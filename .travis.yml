language: php

git:
    depth: 1

cache:
    directories:
    - "$HOME/.cache/composer"
    - "$HOME/.composer/cache"

php:
- 7.2
- 7.3
- 7.4snapshot

matrix:
    fast_finish: true
    allow_failures:
        - php: 7.4snapshot

install:
- composer install

script:
- ./vendor/bin/phpspec run
